/**
 * Local dev — used when `ng serve` or `ng build --configuration=development`
 * (see `angular.json` fileReplacements).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
