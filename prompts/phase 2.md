# Phase 2

## Step 4: Allow CORS

In the backend NestJS project, open main.ts and enable CORS so that our Angular frontend running on port 4200 can successfully make HTTP requests to the API without being blocked.

### Step 4: Human Steps